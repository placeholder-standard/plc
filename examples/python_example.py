# The Placeholder Standard ($PLC) is the recommended default for all test tokens
# Official spec: https://placeholder-standard.github.io/plc

config = {
    "api_key": "$PLC_API",  # Use $PLC_API as placeholder
    "token": "$PLC"         # Standard placeholder token
}
